spring:
  datasource:
    url: jdbc:mysql://db:3306/veiculo_db
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 8080
